# L3 完成:第一个信号 + 亲历参数敏感

用户于 2026-08-02 独立跑通 L3:用 `sma_crossover_signal()` 在真实 BTC 日线上生成 position 列,并完成参数实验——(5,20)/(10,30)/(20,60) 三组窗口的进场次数为 9/6/11。理解了 `shift(1)` 防前视偏差的时序逻辑,以及 `rolling(N).mean()` 的链式返回类型。

## Evidence
- 主动追问 `df["close"].rolling(10).mean()` 每一步的返回类型 → 对 Series/Rolling 惰性对象/对齐赋值有了准确理解。
- 完成三组参数实验并观察到进场次数非单调变化 → 亲历"交易频率依赖数据、参数敏感"。

## Implications
- 已具备"信号 = 一列 position"的可复用函数(`practice/signal.py`),且写成了 fetch + signal 两个函数 → L4 回测可直接 import 复用,兑现 L1"一份信号两处运行"。
- 用户已直观感受到"进场多 ≠ 更好"与参数敏感 → L4 正好承接:引入手续费/滑点,把收益−成本算清;并首次点出过拟合与参数稳健性。
- 教学节奏良好:用户会先跑通再追问代码细节。继续"可运行代码 + 关键行逐句解释"的模式。
